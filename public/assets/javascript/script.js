// during on click event, the text-to-speech button reads the rendered segment
$( "#AlexisButton" ).click(function() {
  responsiveVoice.speak( //pass here the variable of rendered segments
  	);
});
